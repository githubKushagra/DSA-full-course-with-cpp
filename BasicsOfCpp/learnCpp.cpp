#include<iostream>  // iostream is a library which allows us to take input and produce the output based on our input
#include<math.h>
#include<string>

#include<bits/stdc++.h> // bits/stdc++.h - contains all the libraries that are available in cpp and we don't have to write all the libraries seperately, only we have to include this library.

using namespace std;

int main(){

    cout << "This is Kushagra Varshney" << endl;

    // double cgpa;
    // cout << "Enter your cgpa: ";
    // cin >> cgpa;
    // cout << "Your cgpa is " << cgpa << endl;


    string str;
    getline(cin , str);
    cout << "Entered string is : " << str << endl;

    // single quotes ('') is used for char and double quotes ("") is used for string
    char ch = 'k';
    string s = "kushagra";

    return 0;
}